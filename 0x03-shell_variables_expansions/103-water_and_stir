#!/bin/bash

# Store the values of the environment variables in variables
WATER="$WATER"
STIR="$STIR"

# Concatenate the two variables and store the result in a new variable
result="$WATER$STIR"

# Print the result
echo "$result"

